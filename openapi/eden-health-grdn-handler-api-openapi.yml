swagger: '2.0'
info:
  version: "1.0"
  title: Grdn grdn.handler API
  description: Eden Health Inc. primary backend API service.
consumes:
- application/json
produces:
- application/json
tags:
- name: grdn.handler
  description: Configures routes, handlers, and global middleware.
paths:
  /swagger.json:
    get:
      summary: swagger-handler
      description: ''
      tags:
      - grdn.handler
      parameters: []
      responses:
        default:
          description: Default success response.
          schema: {}
