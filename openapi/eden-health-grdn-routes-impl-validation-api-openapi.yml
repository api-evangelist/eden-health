openapi: 3.2.0
info:
  version: null
  title: Grdn.routes.impl.validation API
  description: Eden Health Inc. primary backend API service.
tags:
- name: grdn.routes.impl.validation
  description: null
paths:
  /v2/validate-emails:
    post:
      summary: validate-emails
      description: 'Validates the supplied emails with mailgun. Payload is of form

        [{:email "email1@domain.com" :valid true}

        {:email "e@b.cd" :valid false}]'
      tags:
      - grdn.routes.impl.validation
      responses:
        default:
          description: Default success response.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    email:
                      type: string
                      x-allOf:
                      - type: string
                      - {}
                    valid:
                      type: boolean
                  required:
                  - email
                  - valid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                emails:
                  type: array
                  items:
                    type: string
                    x-allOf:
                    - type: string
                    - {}
              required:
              - emails
              title: grdn.specs.validation/validate-emails-params
        description: 'origin spec: grdn.specs.validation/validate-emails-params'
      operationId: postV2ValidateEmails
      x-operation-id-source: derived
