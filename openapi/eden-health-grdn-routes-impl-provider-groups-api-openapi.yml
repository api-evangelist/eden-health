openapi: 3.2.0
info:
  version: null
  title: Grdn.routes.impl.provider Groups API
  description: Eden Health Inc. primary backend API service.
tags:
- name: grdn.routes.impl.provider-groups
  description: null
paths:
  /v2/provider-groups:
    get:
      summary: get-provider-groups-handler
      description: Get all provider groups.
      tags:
      - grdn.routes.impl.provider-groups
      responses:
        default:
          description: Default success response.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    athena-id:
                      type: integer
                      format: int64
                      minimum: 1
                    name:
                      type: string
                      x-allOf:
                      - type: string
                      - {}
                  required:
                  - athena-id
                  - name
      operationId: getV2ProviderGroups
      x-operation-id-source: derived
